---
phase: 01-pipeline-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/crosspost/__init__.py
  - src/crosspost/config.py
  - src/crosspost/models.py
  - src/crosspost/database.py
  - config.example.yaml
  - tests/conftest.py
  - tests/test_config.py
  - tests/test_models.py
autonomous: true
requirements: [ACQ-03, AUTO-02, AUTO-03]

must_haves:
  truths:
    - "YAML config file loads and validates with type-safe settings (channels, download options, schedule)"
    - "Content table exists with unique video_id, status enum, and all lifecycle fields"
    - "Duplicate video_id insertion is rejected (dedup is persistent)"
    - "State transitions DISCOVERED -> DOWNLOADING -> DOWNLOADED -> FAILED are tracked in SQLite"
  artifacts:
    - path: "pyproject.toml"
      provides: "Project definition with all Phase 1 dependencies"
      contains: "yt-dlp"
    - path: "src/crosspost/config.py"
      provides: "Pydantic Settings with YAML source, channel list, download config, schedule config"
      exports: ["AppSettings", "ChannelConfig", "DownloadConfig", "ScheduleConfig"]
    - path: "src/crosspost/models.py"
      provides: "SQLModel Content table and ContentStatus enum"
      exports: ["Content", "ContentStatus"]
    - path: "src/crosspost/database.py"
      provides: "Engine creation, session context manager, table init"
      exports: ["get_engine", "get_session", "init_db"]
  key_links:
    - from: "src/crosspost/config.py"
      to: "config.example.yaml"
      via: "YamlConfigSettingsSource reads YAML file"
      pattern: "yaml_file.*config\\.yaml"
    - from: "src/crosspost/models.py"
      to: "src/crosspost/database.py"
      via: "SQLModel.metadata.create_all creates Content table"
      pattern: "SQLModel\\.metadata\\.create_all"
---

<objective>
Initialize the CrossPost project and build the data foundation: project scaffolding with uv, Pydantic Settings config loader, SQLModel state machine with Content table, and database session management.

Purpose: Every subsequent plan depends on the config system and database layer. Building these first prevents technical debt.
Output: Working project skeleton with validated config loading and persistent SQLite state machine.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-pipeline-foundation/1-CONTEXT.md
@.planning/phases/01-pipeline-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Project scaffolding, config system, and data models</name>
  <files>
    pyproject.toml
    src/crosspost/__init__.py
    src/crosspost/config.py
    src/crosspost/models.py
    src/crosspost/database.py
    config.example.yaml
    tests/__init__.py
    tests/conftest.py
    tests/test_config.py
    tests/test_models.py
  </files>
  <behavior>
    - test_config: AppSettings loads from YAML file with channels list, download config, schedule config
    - test_config: Default values apply when YAML fields are omitted (max_duration=180, poll_interval_minutes=30)
    - test_config: Invalid YAML raises ValidationError (e.g., negative poll_interval)
    - test_config: Per-channel max_duration override works
    - test_models: Content row can be created with DISCOVERED status and persisted to SQLite
    - test_models: Duplicate video_id raises IntegrityError (unique constraint)
    - test_models: Status can transition DISCOVERED -> DOWNLOADING -> DOWNLOADED
    - test_models: Status can transition DISCOVERED -> DOWNLOADING -> FAILED with error_message
    - test_models: All ContentStatus enum values serialize/deserialize correctly through SQLite
    - test_models: Content fields (video_id, channel_id, title, duration, video_path, etc.) round-trip correctly
  </behavior>
  <action>
    1. Initialize project with uv:
       - `uv init crosspost` in /root/youtube (or manually create pyproject.toml if already in the directory)
       - Use src layout: `src/crosspost/`
       - Set Python requires-python = ">=3.12"
       - Add dependencies: yt-dlp, feedparser, sqlmodel, apscheduler, pydantic-settings[yaml], loguru, tenacity
       - Add dev dependencies: pytest, pytest-mock
       - NOTE: The project root IS /root/youtube. The pyproject.toml goes at /root/youtube/pyproject.toml. The source goes under /root/youtube/src/crosspost/.

    2. Create src/crosspost/__init__.py (empty or with version)

    3. Create src/crosspost/config.py:
       - ChannelConfig(BaseModel): channel_id (str), name (str, default ""), max_duration (int | None, default None for "use global")
       - DownloadConfig(BaseModel): output_dir (str, default "./downloads"), max_duration (int, default 180), format (str, default yt-dlp best mp4), cookies_browser (str, default "firefox")
       - ScheduleConfig(BaseModel): poll_interval_minutes (int, default 30, ge=1), misfire_grace_time (int, default 300)
       - AppSettings(BaseSettings): channels (list[ChannelConfig]), download (DownloadConfig), schedule (ScheduleConfig), database_url (str, default "sqlite:///crosspost.db"), log_level (str, default "INFO")
       - Override settings_customise_sources to use init_settings -> env_settings -> YamlConfigSettingsSource
       - Add a helper method `get_max_duration(channel: ChannelConfig) -> int` that returns channel override or global default

    4. Create src/crosspost/models.py:
       - ContentStatus(str, Enum): DISCOVERED, DOWNLOADING, DOWNLOADED, PROCESSED, TRANSLATED, PUBLISHED, FAILED
       - Content(SQLModel, table=True): id (int PK), video_id (str, unique+indexed), channel_id (str), title (str), description (str), duration (int), thumbnail_url (str), video_url (str), published_at (datetime|None), status (ContentStatus with sa_column=Column(SAEnum)), video_path (str|None), thumbnail_path (str|None), metadata_path (str|None), discovered_at (datetime), downloaded_at (datetime|None), failed_at (datetime|None), error_message (str|None)

    5. Create src/crosspost/database.py:
       - get_engine(database_url: str) -> Engine (with echo=False)
       - init_db(engine: Engine) -> None (SQLModel.metadata.create_all)
       - get_session(engine: Engine) -> context manager yielding Session

    6. Create config.example.yaml with documented example config

    7. Create tests/conftest.py:
       - Fixture: in_memory_engine (SQLite :memory:, creates tables, yields engine)
       - Fixture: session (uses in_memory_engine, yields Session)
       - Fixture: tmp_config_file (writes YAML to tmp_path, returns path)
       - Fixture: sample_content_data (dict with valid Content field values)

    8. Write tests FIRST (RED), then implement (GREEN):
       - tests/test_config.py: Test YAML loading, defaults, validation, per-channel override
       - tests/test_models.py: Test CRUD, dedup (unique constraint), state transitions, enum round-trip
  </action>
  <verify>
    <automated>cd /root/youtube && uv run pytest tests/test_config.py tests/test_models.py -x -v</automated>
  </verify>
  <done>
    - pyproject.toml exists with all dependencies installable via uv
    - AppSettings loads from YAML with validation and defaults
    - Content model persists to SQLite with unique video_id constraint
    - ContentStatus enum round-trips through SQLite correctly
    - All tests pass
  </done>
</task>

</tasks>

<verification>
- `uv run pytest tests/ -x -v` passes all config and model tests
- `uv run python -c "from crosspost.config import AppSettings; from crosspost.models import Content, ContentStatus; print('imports OK')"` succeeds
</verification>

<success_criteria>
- Project initializes with `uv sync` without errors
- Config loads from YAML with type validation
- Content table supports CRUD with dedup and state tracking
- All tests green
</success_criteria>

<output>
After completion, create `.planning/phases/01-pipeline-foundation/01-01-SUMMARY.md`
</output>
